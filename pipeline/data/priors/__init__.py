"""Committed, durable priors for the FPL agent.

This package holds both the capture code and the captured data. The data is
committed to git on purpose: it is derived from information the FPL API
destroys at season rollover, so `predictions/`-style regeneration is not an
option.
"""
